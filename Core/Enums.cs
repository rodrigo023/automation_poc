namespace Core
{
    public enum Browsers
    {
        Chrome,
        Firefox,
        Safari,
        IExplorer,
        Edge
    }

    public enum Platforms
    {
        Tablet,
        Mobile,
        Desktop
    }

    public enum Environments
    {
        Developer,
        Test,
        Staging,
        Live
    }
}
